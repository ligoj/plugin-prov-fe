// Flexible Engine i18n for the prov-fe tool plugin. The legacy `fe.js`
// controller was empty and parameter.csv is empty: the only label the
// legacy nls bundle shipped is the VM `name`.
export default {
  'service:prov:fe:name': 'Name',
}
